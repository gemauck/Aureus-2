# ──────────────────────────────────────────────────────────────────────────────
# Abcotronics ERP – Environment Template
# Copy this file to `.env` (production server) or `.env.local` (local dev) and
# replace placeholder values with your real credentials before starting the app.
# Never commit the filled-in file to source control.
# ──────────────────────────────────────────────────────────────────────────────

# ── Core application settings ────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
APP_NAME="Abcotronics ERP"
APP_URL="https://example.yourdomain.com"
NEXT_PUBLIC_APP_URL="https://example.yourdomain.com"

# ── Authentication / security ────────────────────────────────────────────────
JWT_SECRET=change-me-please
FORCE_SECURE_COOKIES=true
REFRESH_COOKIE_DOMAIN=.yourdomain.com

# ── Database -----------------------------------------------------------------
DATABASE_URL=""
DATABASE_TOKEN=""
DEV_LOCAL_NO_DB=false

# ── Email delivery -----------------------------------------------------------
EMAIL_FROM="no-reply@yourdomain.com"
EMAIL_REPLY_TO="support@yourdomain.com"
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_SECURE=false
SMTP_URL=""
SENDGRID_API_KEY=""
GMAIL_USER=""
GMAIL_APP_PASSWORD=""

# ── Feedback notifications ---------------------------------------------------
FEEDBACK_NOTIFY_EMAIL=""
FEEDBACK_NOTIFY_EMAILS=""
FEEDBACK_ALERT_EMAILS=""
FEEDBACK_ALERT_RECIPIENTS=""

# ── Platform notifications ---------------------------------------------------
ENABLE_LEAVE_EMAIL_NOTIFICATIONS=true

# ── OAuth (Google) -----------------------------------------------------------
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REDIRECT_URI="https://example.yourdomain.com/api/auth/google/callback"

# ── Testing utilities --------------------------------------------------------
TEST_URL="http://localhost:3000"
TEST_EMAIL=""
TEST_PASSWORD=""
TEST_TOKEN=""
TEST_USER_EMAIL=""
USER1_PASSWORD=""
USER2_PASSWORD=""
API_BASE=""
API_TOKEN=""
AUTH_TOKEN=""
LOG_LEVEL=info
RESET_PASSWORD_LENGTH=16

# ── Manufacturing / CRM helpers ---------------------------------------------
OPPORTUNITY_ID=""

# ── Optional host overrides --------------------------------------------------
HOST=""
HOSTNAME=""

# ── Migration / deployment helpers ------------------------------------------
MIGRATION_SECRET_KEY=""










