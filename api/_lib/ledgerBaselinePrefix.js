/**
 * Reference prefix for ledger mismatch repair baselines from
 * `scripts/ledger-cutover-after-stocktake.js --mismatch-only`.
 * Keep aligned when changing cutover dates or reference format.
 */
export const LEDGER_BASELINE_REF_PREFIX = 'LEDGER_BASELINE_2026-04-30'
