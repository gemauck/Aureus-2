DATABASE_URL="postgresql://doadmin:YOUR_PASSWORD_HERE@dbaas-db-6934625-do-user-28031752-0.f.db.ondigitalocean.com:25060/defaultdb?sslmode=require"
JWT_SECRET=0266f788ee2255e2aa973f0984903fb61f3fb1d9f528b315c9dbd0bf53fe5ea8
NODE_ENV=development
PORT=3000

# Document request reply-by-email: inbound address for replies (Resend inbound)
# Set this so "Request documents via email" uses it as Reply-To and reply attachments are routed to the checklist.
DOCUMENT_REQUEST_INBOUND_EMAIL=documents@abcoafrica.co.za

# Document collection: optional BCC on every outbound request/reply; CC allowlist for reply enforcement; inbound match window (days)
# DOCUMENT_COLLECTION_ARCHIVE_BCC=
# DOCUMENT_COLLECTION_CC_EXTRA_ALLOWLIST=
# DOCUMENT_REQUEST_MATCH_MAX_AGE_DAYS=120
# Inbound document replies: subject fallback runs after Message-ID + request number. Set to 0/false/off to disable subject routing entirely.
# DOCUMENT_REQUEST_REPLY_SUBJECT_FALLBACK=1
# When the subject matches the outbound template, narrow candidate senders by project name if it matches exactly one recent sender; strict=1 skips subject routing when that label does not match exactly one sender (recommended multi-site).
# DOCUMENT_REQUEST_REPLY_SUBJECT_STRICT_PROJECT=0

# Resend API key (for sending and for webhook to fetch received email/attachments)
# RESEND_API_KEY=re_xxxxxxxxxxxx

# Resend webhook signing secret (from webhook details in Resend dashboard) - verifies inbound document-request-reply webhook
# RESEND_WEBHOOK_SECRET=whsec_xxxxxxxxxxxx

# Safety Culture API key (for inspections, audits, checklists integration)
# SAFETY_CULTURE_API_KEY=scapi_xxxxxxxxxxxx
