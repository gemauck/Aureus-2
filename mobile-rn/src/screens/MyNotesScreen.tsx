import React from 'react'
import { MyNotesNavigator } from '../userNotes/MyNotesNavigator'

/** My Notes module — personal notes with sharing, tags, and project links. */
export function MyNotesScreen() {
  return <MyNotesNavigator />
}
