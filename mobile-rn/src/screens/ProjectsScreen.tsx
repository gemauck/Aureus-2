import React from 'react'
import { ProjectsNavigator } from '../projects/ProjectsNavigator'

/** Projects module entry — list, detail, tasks, notes, activity. */
export function ProjectsScreen() {
  return <ProjectsNavigator />
}
