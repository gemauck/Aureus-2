import React from 'react'
import { UsersNavigator } from '../users/UsersNavigator'

/** Users module — admin user management, invitations, roles and permissions. */
export function UsersScreen() {
  return <UsersNavigator />
}
