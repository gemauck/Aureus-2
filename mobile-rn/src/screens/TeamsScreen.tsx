import React from 'react'
import { TeamsNavigator } from '../teams/TeamsNavigator'

/** Teams module — knowledge hub, discussions, process flows, meeting notes. */
export function TeamsScreen() {
  return <TeamsNavigator />
}
