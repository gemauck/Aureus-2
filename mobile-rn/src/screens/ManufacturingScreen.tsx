import React from 'react'
import { ManufacturingNavigator } from '../manufacturing/ManufacturingNavigator'

/** Manufacturing module — hub + authenticated web tabs + native field workflows. */
export function ManufacturingScreen() {
  return <ManufacturingNavigator />
}
