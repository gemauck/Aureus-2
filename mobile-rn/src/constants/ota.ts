/** OTA runtime version — must match app.config.js RUNTIME_VERSION. Bump when shipping a new native APK shell. */
export const OTA_RUNTIME_VERSION = 'erp-mobile-4'

export const OTA_CHANNEL = 'production'
