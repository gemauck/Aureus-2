#!/usr/bin/env bash
# Deprecated: self-hosted OTA needs no Expo setup. See mobile-rn/README.md
echo "Self-hosted OTA is enabled — no Expo account required."
echo "Publish JS updates with: npm run mobile:ota:publish"
exit 0
